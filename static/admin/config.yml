backend:
  # name: test-repo
  name: git-gateway
  # repo: hendhyhutomo/gatsbyv2

media_folder: static/assets
public_folder: assets
collections:
  - name: blog
    label: Blog
    folder: src/pages/blog
    create: true
    fields:
      - { name: slug, label: Slug, widget: "string", pattern: [ "([a-z0-9]){3,}","Lowercase and Numerical Only, Minimum Characters = 3"] }
      - { name: index, label: Index, widget: "number", default: 0 }
      - { name: date, label: Date, widget: date }
      - { name: title, label: Title }
      - { name: "coverimage", label: "Cover Image", widget: "image"}
      - { name: body, label: Body, widget: markdown }
